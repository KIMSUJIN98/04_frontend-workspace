1. css 파일 => 외부스타일 연결방법(연결을 어떤 키워드를 통해 어케 했는지 02.css)
외부 스타일 방식 : 스타일 정보만을 따로 기술하는 .css 외부문서를 만들고 link 태그를 통해 연결시켜주는 방식
<link rel="stylesheet" href="resources/css/02.css">

2. html에서 head와 body의 역할 (헤드에 스타일이 들어간다 등등)
<head> 머리부를 나타냄 : 해당 이 문서의 각종 정보와 제목, 설명 및 스크립트, 스타일 시트 내용들 작성
                        <meta>, <title>, <link>, <style>, <script>
<body> 몸체부를 나타냄 : 화면상에 출력해서 보여주고자 하는 모든 정보/내용들을 작성하는 부분

3. h 태그의 종류 (몇부터 몇까지 있는지, 설명 : 가장큰거 두번째 큰거)
- h관련 태그 (h1~h6) : 존재하지 않는 태그 사용시 일반글꼴로 취급

4. html의 기본 구조
<!DOCTYPE html> <!-- HTML 문서 형식 선언 : HTML5 형식 이라는걸 알려주기 위한 태그-->
<html lang="ko"> <!-- html 문서의 시작, 끝을 표시해주는 태그 / lang 속성은 이 페이지가 어느 나라 언어로 되어있는지 표시-->
    <head></head>
    <body>

    </body>
</html>

5. 각종태그들.. br, p, pre, a, img(src alt), ul, ol(ul과 ol의 차이점/ 로마자, 소문자, 3부터 시작하는 등)
<br> <!-- 줄바꿈 태그-->
<hr> <!-- horizon 수평선처럼 선이 그어짐-->
문단을 나누는 태그 : p, pre
<p>
    문단 영역을 나누는 태그로는 p태그와 pre태그가 있다.
    p태그는 줄바꿈 하고자 한다면 별도의 태그를 작성해야됨 <br>
    그리고        공백은 한 개의 공백만을 표시하기 때문에
    별도의 &nbsp;&nbsp;&nbsp;&nbsp; 기호문구를 기술해야됨
</p>

<pre>
    pre태그는 시작태그에서부터 종료태그까지 입력된 내용을 그래도 표현하는 태그
    줄바꿈 이라던가           여러개의 공백을 바로 인식함
</pre>


<img src = "삽입하고자 하는 이미지의 경로" 
[alt = "이미지 설명 문구" width = "가로길이(px,%)" height = "세로길이(px,%)"]

ul : 순서없는 목록 태그
ol : 순서있는 목록 태그 => type 속성을 이용해서 변경 가능(속성값 : 숫자값, A, a, i, I) / start 속성을 이용해서 시작값 변경 가능 / reversed 속성을 통해 역순으로 표기 가능

6. 블럭속성과 인라인 속성 (차이: 어떻게 다른지)
블럭요소 : 한 줄 단위로 영역을 차지함
                  줄바꿈이 적용되어 이미 존재하는 태그의 다음줄에 영역이 잡힘
                  (p, pre, "div", ...)

인라인요소 : content(내용) 영역에 해당하는 부분만 영역을 차지하는 요소
            줄 바꿈이 적용되지 않아 옆으로 영역이 잡힘
            (다음줄로 넘기고자 한다면 직접 br 작성해야함)
            (b, mark, s, img, "span", ...)

블럭요소 : 한 줄로 영역을 차지하고 있는 요소 / 줄바꿈 발생 (div, p, pre, h)
인라인요소 : content 부분만 영역을 차지하고 있는 요소 / 옆으로 배치(span, label, input, img, 글자관련)

7. 표태그 병합 (표를 주면 작성할 줄 알아야함, 병합 / colspan, rowspan이 무엇인지 물어볼 수 있음)
- 표 : 웹 문서에서 자료를 정리할 때 주로 사용
       행, 열로 이루어져 있고, 행과 열이 만나는 지점을 셀이라고함.

- 표를 만드는 태그에는  <table>, <tr>, <th>, <td> 가 있다.
<table></table> : 기본적인 표를 생성해주는 태그
<!-- boeder 속성 : 표의 테두리 두께-->
<!-- caption : 테이블의 제목을 추가하는 태그-->
<tr></tr>       : 표의 한 행을 나타내는 태그
<th></th>       : 표의 제목 셀을 나타내는 태그 => 글자 굵게, 가운데 정렬
<td></td>       : 표의 일반 셀을 나타내는 태그

- 셀(th, td) 태그의 속성을 이용해서 행 또는 열을 합칠 수 있음
colspan="2" : 2개의 열을 합치기
rowspan="2" : 2개의 행을 합치기

8. 아이디랑 클래스의 차이점 (아이디는 하나만 고유해야되고 클래스는 여러개 가능)

9. input type = ?? (text, button, date, ..) 등 타입 속성을 알아야함

